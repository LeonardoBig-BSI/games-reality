import { ReactNode } from "react";

const SectionGiveaway = ({ children }: { children: ReactNode }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default SectionGiveaway